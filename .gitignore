# pioarduino cache directory
/.pio

/.vscode
/.cache
/compile_commands.json
/.clangd

__pycache__

# User-specific InfluxDB upload config (contains API keys/secrets)
# Keep config.example.toml tracked in git instead.
scripts/config.toml
